#開機程式
BIOS將第一扇區的資料讀到記憶體，讀到的程式為bootsect，它的功能是將剩下的kernel導入到記憶體
ps.第一扇區為512bytes
ps.BIOS被設定成會讀取第一扇區的資料並執行，BIOS是主機板廠商寫的，和kernel開發者無關，kernel開發者要寫的是導引程式(bootsect)
設定虛擬環境，將ＯＳ載入到記憶體
# process control
## process creation
## process deletion

# memory management
使用virtual memory
# I/O device

# file system

